schema: ${GRAPHQL_ENDPOINT}
documents: 'src/**/*.{graphql,gql,ts}'
generates:
  src/types/generated.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      skipTypename: false
      withHooks: false
      withComponent: false
      withHOC: false
